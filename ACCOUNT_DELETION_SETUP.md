# 🔐 Инструкция по настройке страницы удаления аккаунта

## 📋 Что требуется от Google Play

Google Play требует, чтобы на странице удаления аккаунта было:

1. ✅ **Упомянуто название приложения или имя разработчика**
2. ✅ **Пошаговая инструкция** о том, как запросить удаление аккаунта
3. ✅ **Информация о данных**, которые будут удалены или сохранены
4. ✅ **Сроки хранения** данных

---

## 📄 Созданные файлы

### 1. `account-deletion.html` — Полная версия с формой

Это готовая HTML страница со всеми необходимыми элементами:
- ✅ Название приложения и разработчика
- ✅ Пошаговая инструкция
- ✅ Список данных, которые будут удалены
- ✅ Сроки обработки
- ✅ Форма для запроса удаления
- ✅ Контактная информация

---

## 🚀 Как использовать

### Вариант 1: Статическая страница (простой способ)

1. **Загрузите файл на ваш сайт:**
   - Например, на GitHub Pages, Netlify, Vercel, или ваш хостинг
   - URL может быть: `https://ваш-сайт.com/account-deletion` или `https://ваш-сайт.com/delete-account`

2. **Откройте файл `account-deletion.html`**

3. **Обновите контактную информацию:**
   - Найдите раздел "Контактная информация"
   - Замените `support@oxia.com` на ваш реальный email

4. **Настройте обработку формы (опционально):**
   - Если хотите, чтобы форма работала, настройте обработчик на вашем сервере
   - Или можно просто указать email для отправки запросов

### Вариант 2: Простая статическая страница (без формы)

Если вы хотите принимать запросы только по email, можно создать упрощенную версию (см. файл `account-deletion-simple.html`).

---

## 📝 Что нужно настроить в HTML файле

### 1. Контактный email

Найдите в файле:
```html
<p><strong>Email:</strong> <a href="mailto:support@oxia.com">support@oxia.com</a></p>
```

Замените `support@oxia.com` на ваш реальный email для поддержки.

### 2. URL для отправки формы (если нужна автоматическая обработка)

В разделе `<script>` найдите функцию `handleSubmit` и настройте отправку на ваш сервер:

```javascript
fetch('/api/account-deletion-request', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        email: email,
        message: document.getElementById('message').value
    })
})
```

Замените `/api/account-deletion-request` на URL вашего API.

---

## 🌐 Где разместить страницу

### Рекомендуемые варианты:

1. **GitHub Pages** (бесплатно)
   - Создайте репозиторий на GitHub
   - Загрузите HTML файл
   - Включите GitHub Pages в настройках
   - URL: `https://ваш-username.github.io/account-deletion.html`

2. **Netlify** (бесплатно)
   - Загрузите файл через drag-and-drop
   - Получите URL автоматически
   - URL: `https://ваш-проект.netlify.app/account-deletion.html`

3. **Vercel** (бесплатно)
   - Загрузите файл через веб-интерфейс
   - Получите URL автоматически

4. **Ваш собственный хостинг**
   - Загрузите файл через FTP или панель управления
   - URL: `https://ваш-сайт.com/account-deletion.html`

---

## 📧 Обработка запросов на удаление

### Вариант 1: Email запросы

Если форма не настроена, пользователи могут отправлять запросы на ваш email. Добавьте на страницу:

```
Отправьте запрос на удаление аккаунта на email: support@ваш-сайт.com
Укажите в письме:
- Email адрес, который использовался для регистрации
- Запрос на удаление аккаунта
```

### Вариант 2: Автоматическая обработка

Если у вас есть backend, настройте API endpoint для обработки запросов:

1. **Создайте API endpoint:**
   ```javascript
   POST /api/account-deletion-request
   Body: {
     email: "user@example.com",
     message: "Optional message"
   }
   ```

2. **Обработайте запрос:**
   - Сохраните запрос в базу данных
   - Отправьте подтверждающее письмо пользователю
   - После подтверждения удалите данные из Supabase

---

## 🔧 Интеграция с Supabase для удаления данных

После получения запроса на удаление, используйте код из `src/services/SupabaseService.ts`:

```typescript
// Удаление всех данных пользователя
await SupabaseService.clearAllUserData();

// Это удалит:
// - Записи PEF
// - Профиль пользователя
// - Настройки пользователя
```

Также нужно удалить сам аккаунт в Supabase Auth:

```typescript
// Удаление аккаунта из Supabase Auth
const { error } = await supabase.auth.admin.deleteUser(userId);
```

---

## ✅ Проверка перед публикацией

Перед тем как указать URL в Google Play Console, убедитесь:

- ✅ На странице указано название приложения "Дневник пикфлоуметрии PeakFlow"
- ✅ Указано имя разработчика "Oxia"
- ✅ Есть пошаговая инструкция (5 шагов)
- ✅ Указано, какие данные будут удалены
- ✅ Указаны сроки обработки (3 дня) и удаления (30 дней)
- ✅ Страница доступна по постоянному URL
- ✅ Контактный email актуален

---

## 📝 Что указать в Google Play Console

В поле "URL для запроса на удаление аккаунта" укажите:

```
https://ваш-сайт.com/account-deletion.html
```

Или другой URL, где размещена страница.

---

## 🔄 Альтернативный вариант: Простая страница без формы

Если вы хотите принимать запросы только по email, создам упрощенную версию страницы (см. `account-deletion-simple.html`).

---

## 📞 Поддержка

Если нужна помощь с настройкой, напишите:
- По настройке формы
- По интеграции с backend
- По размещению на хостинге

---

**Готово! Страница создана и готова к использованию! ✅**
