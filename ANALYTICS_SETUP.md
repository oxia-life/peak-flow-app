# 📊 Настройка аналитики для Peak Flow Diary

## Доступные варианты аналитики

### 1️⃣ **Vercel Analytics** (✅ Уже работает!)

**Не требует установки!** Vercel автоматически собирает базовую аналитику.

**Как посмотреть:**
1. Откройте https://vercel.com/dashboard
2. Выберите проект `peak-flow-app`
3. Нажмите вкладку **Analytics**

**Что показывает:**
- 📈 Количество посещений
- 🌍 География (страны)
- 📱 Устройства (mobile/desktop)
- ⚡ Производительность (время загрузки)
- 🔄 Популярные страницы

---

### 2️⃣ **Supabase Analytics** (✅ Уже работает!)

**Не требует установки!** Supabase собирает аналитику по базе данных.

**Как посмотреть:**
1. Откройте https://supabase.com
2. Выберите ваш проект
3. Смотрите:
   - **Authentication → Users** - пользователи
   - **Database → Logs** - логи запросов
   - **Project Settings → Usage** - использование ресурсов

**Что показывает:**
- 👥 Количество пользователей
- 🔐 Авторизации
- 💾 Размер базы данных
- 📊 API запросы
- 🔥 Активность пользователей

---

### 3️⃣ **Google Analytics 4** (Нужна установка)

Для детальной аналитики поведения пользователей.

#### **Шаг 1: Создайте Google Analytics аккаунт**

1. Откройте https://analytics.google.com
2. Нажмите **"Start measuring"**
3. **Account name:** "Oxia"
4. **Property name:** "Peak Flow Diary"
5. **Industry:** Healthcare
6. **Business size:** Small
7. **Web stream URL:** `https://peakflow.oxia.life`

Google выдаст вам **Measurement ID** вида: `G-XXXXXXXXXX`

#### **Шаг 2: Установите пакет**

```bash
cd /Users/monastyrskaya/Documents/peak-flow-diary
npm install react-ga4
```

#### **Шаг 3: Добавьте в код**

Откройте `App.tsx` и добавьте в начало файла:

```typescript
import ReactGA from 'react-ga4';

// Инициализация Google Analytics
if (typeof window !== 'undefined') {
  ReactGA.initialize('G-XXXXXXXXXX'); // Замените на ваш ID
  ReactGA.send('pageview');
}
```

#### **Шаг 4: Добавьте переменную окружения в Vercel**

1. Vercel Dashboard → Settings → Environment Variables
2. Добавьте:
   ```
   Name:  EXPO_PUBLIC_GA_MEASUREMENT_ID
   Value: G-XXXXXXXXXX
   ```

3. Обновите код:
   ```typescript
   if (typeof window !== 'undefined' && process.env.EXPO_PUBLIC_GA_MEASUREMENT_ID) {
     ReactGA.initialize(process.env.EXPO_PUBLIC_GA_MEASUREMENT_ID);
     ReactGA.send('pageview');
   }
   ```

#### **Шаг 5: Задеплойте обновление**

```bash
git add .
git commit -m "Add Google Analytics"
git push
```

Vercel автоматически задеплоит обновление.

**Что покажет Google Analytics:**
- 📱 Откуда приходят пользователи (источники трафика)
- ⏱️ Сколько времени проводят
- 🔄 Какие экраны просматривают
- 📉 Где уходят (bounce rate)
- 👥 Демография (возраст, пол, интересы)
- 🎯 Конверсии и цели

---

### 4️⃣ **Медицинская аналитика (специфичная для вашего приложения)**

Вы можете отслеживать медицинские метрики прямо в Supabase!

#### **Создайте представление (View) в Supabase:**

Выполните в SQL Editor:

```sql
-- Статистика пользователей
CREATE OR REPLACE VIEW user_statistics AS
SELECT 
  COUNT(DISTINCT user_id) as total_users,
  COUNT(DISTINCT CASE WHEN created_at >= NOW() - INTERVAL '7 days' THEN user_id END) as weekly_active_users,
  COUNT(DISTINCT CASE WHEN created_at >= NOW() - INTERVAL '30 days' THEN user_id END) as monthly_active_users
FROM profiles;

-- Статистика измерений
CREATE OR REPLACE VIEW measurement_statistics AS
SELECT 
  COUNT(*) as total_measurements,
  COUNT(DISTINCT user_id) as users_with_measurements,
  AVG(value) as avg_pef_value,
  MIN(value) as min_pef_value,
  MAX(value) as max_pef_value,
  COUNT(CASE WHEN cough = true THEN 1 END) as measurements_with_cough,
  COUNT(CASE WHEN breathlessness = true THEN 1 END) as measurements_with_breathlessness,
  COUNT(CASE WHEN sputum = true THEN 1 END) as measurements_with_sputum
FROM pef_records;

-- Активность по дням
CREATE OR REPLACE VIEW daily_activity AS
SELECT 
  DATE(created_at) as date,
  COUNT(DISTINCT user_id) as active_users,
  COUNT(*) as measurements_count,
  AVG(value) as avg_pef
FROM pef_records
WHERE created_at >= NOW() - INTERVAL '30 days'
GROUP BY DATE(created_at)
ORDER BY date DESC;
```

#### **Как посмотреть эти метрики:**

1. Supabase → Table Editor
2. В списке таблиц найдите **Views** (представления)
3. Откройте `user_statistics`, `measurement_statistics`, `daily_activity`

**Это покажет:**
- 👥 Сколько пользователей зарегистрировано
- 📊 Сколько измерений сделано
- 📈 Средние показатели ПСВ
- 🚨 Частота симптомов
- 📅 Активность по дням

---

## 🎯 **Рекомендация для старта:**

### **Используйте сейчас (бесплатно, ничего не делать):**
1. ✅ **Vercel Analytics** - для веб-трафика
2. ✅ **Supabase Dashboard** - для пользователей и данных

### **Добавьте позже (если нужна детальная аналитика):**
3. **Google Analytics** - для глубокого анализа поведения
4. **Custom Views в Supabase** - для медицинской статистики

---

## 📊 **Чек-лист: Что смотреть каждый день/неделю**

### **Ежедневно:**
- [ ] Vercel → Analytics → посещения за день
- [ ] Supabase → Authentication → Users → новые регистрации
- [ ] Vercel → Deployments → ошибки деплоя (если есть)

### **Еженедельно:**
- [ ] Vercel → Analytics → тренды за неделю
- [ ] Supabase → Usage → использование ресурсов
- [ ] Supabase → Database → размер базы данных

### **Ежемесячно:**
- [ ] Общее количество пользователей
- [ ] Retention (сколько пользователей возвращаются)
- [ ] Средние показатели ПСВ по базе
- [ ] Наиболее активные часы/дни

---

## 🚨 **Что делать, если видите проблемы:**

### **Много ошибок 4xx/5xx:**
- Проверьте Vercel → Logs
- Проверьте Supabase → Database → Logs

### **Медленная загрузка:**
- Vercel → Analytics → Performance
- Оптимизируйте изображения
- Включите кэширование (уже настроено в vercel.json)

### **Пользователи уходят после регистрации:**
- Проверьте UX онбординга
- Убедитесь, что авторизация работает корректно
- Добавьте подсказки для новых пользователей

---

## 📈 **Дополнительные инструменты (опционально):**

### **Для отслеживания ошибок:**
- **Sentry** - https://sentry.io (бесплатно до 5K событий/месяц)
- Отслеживает JavaScript ошибки в реальном времени

### **Для A/B тестирования:**
- **Google Optimize** (бесплатно)
- Тестирование разных версий UI

### **Для обратной связи:**
- **Hotjar** - записи сессий пользователей (бесплатно до 35 сессий/день)
- **UserVoice** - сбор отзывов

---

## ✅ **Итого: Что делать прямо сейчас**

1. **Откройте Vercel Analytics** - уже работает! ✨
   https://vercel.com/dashboard → ваш проект → Analytics

2. **Откройте Supabase Dashboard** - уже работает! ✨
   https://supabase.com → ваш проект → Authentication → Users

3. **Подождите 24 часа** - накопятся первые данные 📊

4. **Расскажите друзьям/коллегам** - попросите протестировать приложение

5. **Проверьте аналитику через неделю** - увидите первые тренды 📈

---

## 🎉 **Поздравляю с запуском!**

Теперь вы можете:
- ✅ Видеть, сколько людей используют приложение
- ✅ Отслеживать технические проблемы
- ✅ Анализировать поведение пользователей
- ✅ Принимать решения на основе данных

**Следующий шаг:** Поделитесь ссылкой `https://peakflow.oxia.life` и наблюдайте за ростом! 🚀

---

## 🆘 **Нужна помощь?**

Если хотите настроить дополнительную аналитику или нужна помощь с интерпретацией данных - пишите!

**Удачи с проектом! 📊✨**

