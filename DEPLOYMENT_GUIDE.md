# 🚀 Инструкция по публикации на peakflow.oxia.life

## 📋 Оглавление
1. [Предварительная подготовка](#1-предварительная-подготовка)
2. [Выбор платформы хостинга](#2-выбор-платформы-хостинга)
3. [Настройка безопасности](#3-настройка-безопасности)
4. [Сборка и деплой](#4-сборка-и-деплой)
5. [Настройка домена](#5-настройка-домена)
6. [Настройка Supabase](#6-настройка-supabase)
7. [Проверка безопасности](#7-проверка-безопасности)
8. [Мониторинг и поддержка](#8-мониторинг-и-поддержка)

---

## 1. Предварительная подготовка

### ✅ Проверьте защиту данных

**Убедитесь, что `.env` в `.gitignore`:**

```bash
cd /Users/monastyrskaya/Documents/peak-flow-diary
cat .gitignore | grep .env
```

Если `.env` нет в `.gitignore`, это **КРИТИЧНО** исправить!

**Проверьте, что `.env` не попал в Git:**

```bash
git log --all --full-history -- .env
```

Если файл был закоммичен, **немедленно:**
1. Смените ключи в Supabase
2. Удалите `.env` из истории Git
3. Добавьте в `.gitignore`

---

## 2. Выбор платформы хостинга

### 🏆 Рекомендация: **Vercel** (оптимально для вашего случая)

**Почему Vercel:**
- ✅ Бесплатный SSL-сертификат (автоматически)
- ✅ Поддержка кастомных доменов
- ✅ Автоматический деплой из Git
- ✅ Защита от DDoS
- ✅ CDN для быстрой загрузки
- ✅ Поддержка переменных окружения
- ✅ Автоматические бэкапы
- ✅ 100GB бесплатного трафика/месяц

**Альтернативы:**
- **Netlify** - аналогично Vercel
- **Cloudflare Pages** - лучшая защита от атак
- **GitHub Pages** - проще, но нет поддержки переменных окружения

---

## 3. Настройка безопасности

### 🔐 Шаг 1: Проверка переменных окружения

**Важно:** Никогда не коммитьте `.env` файл!

Проверьте ваш `.env`:
```bash
cat .env
```

Должно быть:
```
EXPO_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Только `EXPO_PUBLIC_*` переменные попадут в веб-сборку!**

### 🔐 Шаг 2: Обновите `.gitignore`

Убедитесь, что в `.gitignore` есть:
```
.env
.env.local
.env.production
.env.development
web-build/
dist/
```

### 🔐 Шаг 3: Настройте Content Security Policy

Создайте файл для заголовков безопасности:

**Для Vercel:** создайте `vercel.json`
**Для Netlify:** создайте `netlify.toml`
**Для Cloudflare:** настройте в панели управления

Я создам эти файлы далее.

---

## 4. Сборка и деплой

### 📦 Вариант A: Деплой через Vercel (РЕКОМЕНДУЕТСЯ)

#### Шаг 1: Установка Vercel CLI

```bash
npm install -g vercel
```

#### Шаг 2: Сборка веб-версии

```bash
cd /Users/monastyrskaya/Documents/peak-flow-diary
npm run build:web
```

Эта команда создаст папку `web-build/` с готовым приложением.

#### Шаг 3: Логин в Vercel

```bash
vercel login
```

Выберите удобный метод авторизации (GitHub, GitLab, Email).

#### Шаг 4: Деплой

```bash
cd web-build
vercel --prod
```

**Vercel спросит:**
1. **Set up and deploy?** → Yes
2. **Which scope?** → Ваш аккаунт
3. **Link to existing project?** → No (первый раз)
4. **Project name?** → peak-flow-diary
5. **Directory?** → `./` (уже в web-build)

Vercel выдаст вам временный URL: `https://peak-flow-diary.vercel.app`

#### Шаг 5: Добавьте переменные окружения в Vercel

```bash
# Через CLI
vercel env add EXPO_PUBLIC_SUPABASE_URL
# Вставьте значение из .env

vercel env add EXPO_PUBLIC_SUPABASE_ANON_KEY
# Вставьте значение из .env
```

**Или через веб-интерфейс:**
1. Откройте https://vercel.com/dashboard
2. Выберите ваш проект `peak-flow-diary`
3. Settings → Environment Variables
4. Добавьте:
   - `EXPO_PUBLIC_SUPABASE_URL` = ваш URL
   - `EXPO_PUBLIC_SUPABASE_ANON_KEY` = ваш ключ

#### Шаг 6: Передеплой с переменными окружения

```bash
cd ..
npm run build:web
cd web-build
vercel --prod
```

---

### 📦 Вариант B: Деплой через Netlify

#### Шаг 1: Установка Netlify CLI

```bash
npm install -g netlify-cli
```

#### Шаг 2: Сборка и деплой

```bash
npm run build:web
netlify login
netlify deploy --prod --dir=web-build
```

#### Шаг 3: Добавьте переменные окружения

1. Откройте https://app.netlify.com
2. Выберите сайт
3. Site settings → Environment variables
4. Добавьте переменные

---

## 5. Настройка домена

### 🌐 Подключение peakflow.oxia.life

#### Для Vercel:

1. **В панели Vercel:**
   - Откройте проект → Settings → Domains
   - Нажмите "Add Domain"
   - Введите: `peakflow.oxia.life`
   - Vercel покажет, какие DNS-записи нужно добавить

2. **В панели управления доменом oxia.life:**
   
   Добавьте **CNAME** запись:
   ```
   Type:  CNAME
   Name:  peakflow
   Value: cname.vercel-dns.com
   TTL:   Auto или 300
   ```

   **Или A-запись:**
   ```
   Type:  A
   Name:  peakflow
   Value: 76.76.21.21
   TTL:   Auto или 300
   ```

3. **Подождите 5-30 минут** для распространения DNS

4. **Vercel автоматически настроит SSL** (Let's Encrypt)

#### Для Netlify:

1. **В панели Netlify:**
   - Domain settings → Add custom domain
   - Введите: `peakflow.oxia.life`

2. **Добавьте CNAME:**
   ```
   Type:  CNAME
   Name:  peakflow
   Value: ваш-сайт.netlify.app
   ```

3. **SSL настроится автоматически**

---

## 6. Настройка Supabase

### 🔗 Обновите Redirect URLs для Magic Link

**КРИТИЧНО для авторизации!**

1. Откройте https://supabase.com → ваш проект
2. **Authentication → URL Configuration**
3. Добавьте в **Site URL:**
   ```
   https://peakflow.oxia.life
   ```

4. Добавьте в **Redirect URLs:**
   ```
   https://peakflow.oxia.life
   https://peakflow.oxia.life/
   https://peakflow.oxia.life/**
   https://peakflow.oxia.life/auth/callback
   ```

5. **Сохраните изменения**

### 🛡️ Настройте Row Level Security (RLS)

Убедитесь, что RLS включен для всех таблиц:

```sql
-- Проверьте в SQL Editor Supabase
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'public';
```

Все должно быть `true`. Если нет — **это критическая уязвимость!**

### 🔒 Проверьте политики безопасности

В Supabase → Table Editor → выберите таблицу → RLS policies

Убедитесь, что политики правильные (как в `SUPABASE_SETUP.md`).

---

## 7. Проверка безопасности

### ✅ Чек-лист безопасности перед запуском

- [ ] `.env` файл в `.gitignore`
- [ ] `.env` не закоммичен в Git
- [ ] Переменные окружения добавлены в Vercel/Netlify
- [ ] HTTPS работает (проверьте 🔒 в адресной строке)
- [ ] RLS включен для всех таблиц в Supabase
- [ ] Политики безопасности настроены
- [ ] Redirect URLs обновлены в Supabase
- [ ] Service Worker работает
- [ ] PWA устанавливается на телефон

### 🔍 Тестирование безопасности

1. **Проверьте, что данные защищены:**
   - Откройте DevTools → Network
   - Попробуйте получить данные другого пользователя (должна быть ошибка 403)

2. **Проверьте HTTPS:**
   ```bash
   curl -I https://peakflow.oxia.life
   ```
   Должно быть `200 OK` и заголовки безопасности.

3. **SSL Labs Test:**
   - Откройте: https://www.ssllabs.com/ssltest/
   - Проверьте: `peakflow.oxia.life`
   - Цель: оценка **A** или **A+**

4. **Security Headers:**
   - Откройте: https://securityheaders.com/
   - Проверьте: `peakflow.oxia.life`

---

## 8. Мониторинг и поддержка

### 📊 Настройте мониторинг

**Vercel Dashboard:**
- Analytics → следите за посещаемостью
- Logs → проверяйте ошибки

**Supabase Dashboard:**
- Database → Health → мониторинг нагрузки
- Logs → проверяйте подозрительную активность

### 🔔 Настройте оповещения

1. **Vercel:**
   - Settings → Notifications
   - Включите уведомления о сбоях

2. **Supabase:**
   - Project settings → Email notifications
   - Включите уведомления о проблемах

### 🔄 Обновление приложения

**Автоматический деплой из Git (рекомендуется):**

1. Создайте репозиторий на GitHub
2. Подключите Vercel к репозиторию
3. При каждом `git push` → автоматический деплой

**Ручной деплой:**

```bash
cd /Users/monastyrskaya/Documents/peak-flow-diary
npm run build:web
cd web-build
vercel --prod
```

---

## 🚨 Что делать при проблемах

### Проблема: "Не работает авторизация"

**Решение:**
1. Проверьте Redirect URLs в Supabase
2. Проверьте HTTPS (должен быть SSL)
3. Проверьте консоль браузера на ошибки

### Проблема: "Данные не сохраняются"

**Решение:**
1. Проверьте RLS политики в Supabase
2. Проверьте переменные окружения в Vercel
3. Проверьте консоль → ошибки API

### Проблема: "PWA не устанавливается"

**Решение:**
1. Убедитесь, что HTTPS работает
2. Проверьте manifest.json доступен
3. Проверьте Service Worker в DevTools

### Проблема: "Медленная загрузка"

**Решение:**
1. Vercel автоматически использует CDN
2. Проверьте размер изображений (должны быть оптимизированы)
3. Включите кэширование в Vercel

---

## 📚 Дополнительные улучшения

### После успешного запуска:

1. **Добавьте Google Analytics** (опционально)
2. **Настройте Sentry** для отслеживания ошибок
3. **Добавьте политику конфиденциальности** (для медицинских данных важно!)
4. **Создайте страницу "О приложении"** с контактами
5. **Настройте бэкапы** в Supabase (ежедневные)

---

## ✅ Итоговый чек-лист запуска

- [ ] Собрали веб-версию: `npm run build:web`
- [ ] Задеплоили на Vercel: `vercel --prod`
- [ ] Добавили переменные окружения в Vercel
- [ ] Настроили домен `peakflow.oxia.life`
- [ ] Обновили Redirect URLs в Supabase
- [ ] Проверили HTTPS работает
- [ ] Проверили RLS включен
- [ ] Протестировали авторизацию
- [ ] Протестировали сохранение данных
- [ ] Проверили PWA устанавливается
- [ ] Проверили на мобильных устройствах
- [ ] Настроили мониторинг

---

## 🎉 Поздравляю!

Ваше приложение теперь доступно на `https://peakflow.oxia.life`

**Следующие шаги:**
1. Протестируйте на разных устройствах
2. Попросите кого-то попробовать
3. Мониторьте ошибки первые дни
4. Соберите обратную связь

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте логи в Vercel Dashboard
3. Проверьте логи в Supabase Dashboard
4. Напишите мне в чат с описанием ошибки

**Удачи с запуском! 🚀**


