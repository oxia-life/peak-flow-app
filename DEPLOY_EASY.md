# 🚀 ПРОСТОЙ деплой через веб-интерфейс (БЕЗ установок!)

## 📋 Способ для тех, у кого проблемы с npm/CLI

Vercel сам соберёт и задеплоит ваше приложение! Просто следуйте инструкции.

---

## Шаг 1: Создайте GitHub репозиторий (5 минут)

### 1.1 Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. **Repository name:** `peak-flow-diary`
3. **Visibility:** ✅ **Private** (для безопасности медицинских данных)
4. **НЕ добавляйте** README, .gitignore, license
5. Нажмите **Create repository**

### 1.2 Загрузите код в GitHub

**В терминале выполните:**

```bash
cd /Users/monastyrskaya/Documents/peak-flow-diary

# Инициализируйте Git (если ещё не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit"

# Подключите к GitHub (замените ВАШ-USERNAME на ваш username GitHub)
git remote add origin https://github.com/ВАШ-USERNAME/peak-flow-diary.git

# Загрузите код
git branch -M main
git push -u origin main
```

**⚠️ ВАЖНО:** Убедитесь, что `.env` в `.gitignore` и **НЕ попал в Git**!

```bash
# Проверьте
cat .gitignore | grep .env

# Если .env НЕТ, добавьте:
echo ".env" >> .gitignore
echo "web-build/" >> .gitignore
git add .gitignore
git commit -m "Add .env to gitignore"
git push
```

---

## Шаг 2: Подключите Vercel (5 минут)

### 2.1 Зарегистрируйтесь в Vercel

1. Откройте https://vercel.com/signup
2. Нажмите **Continue with GitHub**
3. Авторизуйте Vercel

### 2.2 Импортируйте проект

1. В Vercel Dashboard нажмите **Add New** → **Project**
2. Найдите репозиторий `peak-flow-diary`
3. Нажмите **Import**

### 2.3 Настройте сборку

**Framework Preset:** Выберите **Other** (или Expo если есть)

**Build and Output Settings:**
- Build Command: `npx expo export --platform web`
- Output Directory: `dist`
- Install Command: `npm install`

**Root Directory:** `.` (оставить как есть)

### 2.4 Добавьте переменные окружения

Нажмите **Environment Variables** и добавьте:

**Переменная 1:**
- **Key:** `EXPO_PUBLIC_SUPABASE_URL`
- **Value:** `https://qppahjfutnvtslyeblno.supabase.co` (ваш URL)
- **Environment:** Production ✅

**Переменная 2:**
- **Key:** `EXPO_PUBLIC_SUPABASE_ANON_KEY`
- **Value:** (ваш anon key из `.env`)
- **Environment:** Production ✅

### 2.5 Запустите деплой

Нажмите **Deploy** и подождите 2-5 минут.

Vercel:
- ✅ Установит зависимости
- ✅ Соберёт приложение
- ✅ Задеплоит на сервер
- ✅ Выдаст URL: `https://peak-flow-diary-xxxxx.vercel.app`

---

## Шаг 3: Подключите домен peakflow.oxia.life (5 минут)

### 3.1 В Vercel Dashboard

1. Откройте ваш проект
2. **Settings** → **Domains**
3. Нажмите **Add**
4. Введите: `peakflow.oxia.life`
5. Нажмите **Add**

Vercel покажет, какую DNS запись нужно добавить.

### 3.2 В панели управления доменом oxia.life

Добавьте **CNAME запись:**

```
Type:  CNAME
Name:  peakflow
Value: cname.vercel-dns.com
TTL:   300
```

**Подождите 5-30 минут** для распространения DNS.

---

## Шаг 4: Обновите Supabase (2 минуты)

1. Откройте https://supabase.com → ваш проект
2. **Authentication** → **URL Configuration**
3. **Site URL:** `https://peakflow.oxia.life`
4. **Redirect URLs** → добавьте:
   ```
   https://peakflow.oxia.life
   https://peakflow.oxia.life/**
   ```
5. **Save**

---

## Шаг 5: Проверка (2 минуты)

1. Откройте https://peakflow.oxia.life
2. Проверьте:
   - [ ] Сайт открывается
   - [ ] HTTPS работает (🔒)
   - [ ] Авторизация работает
   - [ ] Данные сохраняются

---

## ✅ Готово!

Теперь при каждом `git push` в main:
- ✨ Vercel автоматически пересоберёт и задеплоит приложение
- ⚡ Деплой займет 2-3 минуты
- 🔔 Вы получите уведомление

---

## 🔄 Обновление приложения в будущем

Просто делайте изменения и пушьте в GitHub:

```bash
# Внесли изменения в код
git add .
git commit -m "Описание изменений"
git push

# Vercel автоматически задеплоит! ✨
```

---

## 🆘 Если что-то не работает

### Проблема: "Build failed"

**Решение:**
1. Откройте Vercel Dashboard → ваш проект → Deployments
2. Нажмите на Failed deployment
3. Посмотрите логи сборки
4. Чаще всего проблема в:
   - Отсутствие переменных окружения
   - Неправильная команда сборки
   - Отсутствие зависимостей

### Проблема: "Авторизация не работает"

**Решение:**
1. Проверьте Redirect URLs в Supabase
2. Проверьте переменные окружения в Vercel
3. Проверьте консоль браузера (F12)

### Проблема: "Домен не подключается"

**Решение:**
1. Подождите 30 минут (DNS)
2. Проверьте CNAME запись: `dig peakflow.oxia.life`
3. Убедитесь, что в Vercel статус домена "Valid"

---

## 📚 Преимущества этого способа

✅ **Автоматический деплой** при каждом git push
✅ **Не нужно устанавливать** Vercel CLI
✅ **Не нужно собирать** локально
✅ **Не нужны права администратора**
✅ **GitHub → бэкапы кода** бесплатно
✅ **Preview deployments** для тестирования
✅ **Откат на предыдущую версию** в 1 клик

---

## 🎉 Поздравляю!

Ваше приложение на `https://peakflow.oxia.life` и автоматически обновляется! 🚀

**Следующие шаги:**
1. Протестируйте на разных устройствах
2. Установите PWA на телефон
3. Соберите обратную связь

**Удачи! ✨**

