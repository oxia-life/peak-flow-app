# 🚀 Быстрый старт: Публикация на peakflow.oxia.life

## 📋 Что нужно сделать (30 минут)

### Шаг 1: Подготовка (5 минут)

```bash
cd /Users/monastyrskaya/Documents/peak-flow-diary

# Проверьте, что .env в безопасности
cat .gitignore | grep .env

# Если .env НЕТ в .gitignore - ДОБАВЬТЕ!
echo ".env" >> .gitignore
echo "web-build/" >> .gitignore
```

---

### Шаг 2: Установка Vercel (2 минуты)

```bash
npm install -g vercel
```

---

### Шаг 3: Сборка приложения (3 минуты)

```bash
npm run build:web
```

Подождите, пока соберется. Появится папка `web-build/`.

---

### Шаг 4: Деплой на Vercel (5 минут)

```bash
# Войдите в Vercel
vercel login

# Задеплойте
cd web-build
vercel --prod
```

**Vercel спросит:**
- Set up and deploy? → **Yes**
- Which scope? → Выберите ваш аккаунт
- Link to existing project? → **No**
- Project name? → **peak-flow-diary**
- Directory? → **./** (оставить как есть)

Vercel выдаст временный URL: `https://peak-flow-diary-xxxxx.vercel.app`

---

### Шаг 5: Добавьте переменные окружения (5 минут)

**Вариант A: Через веб-интерфейс (проще)**

1. Откройте https://vercel.com/dashboard
2. Выберите проект `peak-flow-diary`
3. Settings → Environment Variables
4. Добавьте:
   - Key: `EXPO_PUBLIC_SUPABASE_URL`
   - Value: (скопируйте из вашего `.env`)
   - Apply to: Production
   - Save
   
5. Добавьте:
   - Key: `EXPO_PUBLIC_SUPABASE_ANON_KEY`
   - Value: (скопируйте из вашего `.env`)
   - Apply to: Production
   - Save

**Вариант B: Через CLI**

```bash
vercel env add EXPO_PUBLIC_SUPABASE_URL
# Вставьте значение

vercel env add EXPO_PUBLIC_SUPABASE_ANON_KEY
# Вставьте значение
```

---

### Шаг 6: Передеплой с переменными (2 минуты)

```bash
cd ..
npm run build:web
cd web-build
vercel --prod
```

---

### Шаг 7: Подключите домен peakflow.oxia.life (10 минут)

#### В панели Vercel:

1. Откройте проект → **Settings** → **Domains**
2. Нажмите **Add Domain**
3. Введите: `peakflow.oxia.life`
4. Vercel покажет инструкцию

#### В панели управления доменом oxia.life:

Добавьте **CNAME запись:**

```
Type:  CNAME
Name:  peakflow
Value: cname.vercel-dns.com
TTL:   300 (или Auto)
```

**Подождите 5-30 минут** для распространения DNS.

---

### Шаг 8: Обновите Supabase (5 минут)

1. Откройте https://supabase.com → ваш проект
2. **Authentication** → **URL Configuration**
3. **Site URL** → измените на: `https://peakflow.oxia.life`
4. **Redirect URLs** → добавьте:
   ```
   https://peakflow.oxia.life
   https://peakflow.oxia.life/**
   ```
5. **Save**

---

### Шаг 9: Проверка (5 минут)

```bash
# Проверьте, что сайт работает
curl -I https://peakflow.oxia.life

# Откройте в браузере
open https://peakflow.oxia.life
```

**Проверьте:**
- [ ] Сайт открывается
- [ ] HTTPS работает (🔒 в адресной строке)
- [ ] Авторизация работает
- [ ] Данные сохраняются
- [ ] PWA устанавливается на телефон

---

## ✅ Готово!

Ваше приложение работает на `https://peakflow.oxia.life`! 🎉

---

## 🔒 Проверка безопасности

После деплоя **ОБЯЗАТЕЛЬНО** проверьте:

```bash
# 1. Проверьте SSL
curl -I https://peakflow.oxia.life | grep strict-transport-security

# 2. Проверьте заголовки безопасности
curl -I https://peakflow.oxia.life | grep -E "X-Frame|X-Content|X-XSS"
```

**Онлайн проверки:**
1. SSL: https://www.ssllabs.com/ssltest/analyze.html?d=peakflow.oxia.life
2. Security Headers: https://securityheaders.com/?q=peakflow.oxia.life

**Цель:** Оценки **A** или выше.

---

## 🆘 Если что-то не работает

### Проблема: "Авторизация не работает"

**Решение:**
1. Проверьте Redirect URLs в Supabase
2. Проверьте переменные окружения в Vercel
3. Проверьте консоль браузера (F12)

### Проблема: "Домен не открывается"

**Решение:**
1. Подождите 30 минут (DNS может занять время)
2. Проверьте CNAME запись: `dig peakflow.oxia.life`
3. Проверьте в Vercel Dashboard → Domains

### Проблема: "Данные не сохраняются"

**Решение:**
1. Проверьте RLS включен в Supabase
2. Проверьте политики безопасности
3. Проверьте консоль → Network → ошибки API

---

## 📚 Детальная документация

- **`DEPLOYMENT_GUIDE.md`** - полная инструкция
- **`SECURITY_CHECKLIST.md`** - чек-лист безопасности
- **`SUPABASE_SETUP.md`** - настройка Supabase

---

## 🔄 Обновление приложения в будущем

**Автоматический деплой (рекомендуется):**

1. Создайте репозиторий на GitHub:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/peak-flow-diary.git
   git push -u origin main
   ```

2. В Vercel Dashboard:
   - Settings → Git
   - Connect GitHub repository
   - Каждый `git push` → автоматический деплой ✨

**Ручное обновление:**

```bash
npm run build:web
cd web-build
vercel --prod
```

---

## 🎉 Поздравляю с запуском!

Теперь ваше приложение доступно в интернете! 

**Следующие шаги:**
1. Протестируйте на разных устройствах
2. Установите PWA на телефон
3. Попросите кого-то попробовать
4. Соберите обратную связь

**Удачи! 🚀**


