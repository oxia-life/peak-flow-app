# Cursor Rules для Peak Flow Diary

## 🔴 КРИТИЧЕСКИЕ ПРАВИЛА:

1. **main ветка = ТОЛЬКО ВЕБ**
   - НЕ добавлять мобильно-специфичные фичи в main
   - main автоматически деплоится на peakflow.oxia.life

2. **Мобильная разработка = отдельные ветки**
   - mobile/staging - для разработки и тестирования
   - mobile/production - для релизов в App Store / Google Play

3. **Перед push в main ВСЕГДА:**
   - Проверьте, что изменения не ломают веб
   - Протестируйте локально: npm run web
   - Убедитесь, что нет мобильно-специфичного кода

4. **Platform-specific код:**
   - Всегда используйте Platform.OS проверки
   - Создавайте .web.tsx / .ios.tsx / .android.tsx файлы при необходимости

## Структура веток:
- main → веб (автодеплой)
- mobile/staging → мобильная разработка
- mobile/production → мобильные релизы
- feature/* → временные фичи

## Команды:
- npm run web → тестирование веба
- npm start → тестирование мобильных
- eas build → сборка мобильных приложений
- git push origin main → деплой веба


