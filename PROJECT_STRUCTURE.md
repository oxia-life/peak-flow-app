# Структура проекта "Дневник пикфлоуметрии"

## 📁 Обзор файловой структуры

```
peal flow/
│
├── App.tsx                      # Точка входа приложения
├── app.json                     # Конфигурация Expo
├── package.json                 # Зависимости проекта
├── tsconfig.json               # Конфигурация TypeScript
├── babel.config.js             # Конфигурация Babel
├── metro.config.js             # Конфигурация Metro bundler
│
├── .gitignore                  # Игнорируемые Git файлы
├── .npmrc                      # Настройки npm
├── .prettierrc                 # Настройки форматирования кода
├── .eslintrc.js               # Настройки линтера
│
├── README.md                   # Основная документация
├── QUICKSTART.md              # Инструкции для быстрого старта
├── PROJECT_STRUCTURE.md       # Этот файл
│
├── assets/                     # Ресурсы приложения
│   └── README.md              # Инструкции по добавлению иконок
│
└── src/                        # Исходный код приложения
    │
    ├── AppNavigator.tsx        # 🧭 Корневая навигация
    │
    ├── types/                  # 📝 TypeScript типы и интерфейсы
    │   └── models.ts          # Модели данных (Profile, PEFRecord)
    │
    ├── utils/                  # 🛠 Утилиты
    │   └── dateUtils.ts       # Работа с датами
    │
    ├── services/               # 🔧 Сервисы
    │   ├── Storage.ts         # Работа с AsyncStorage
    │   └── NotificationsService.ts  # Заглушка для уведомлений
    │
    ├── components/             # 🧩 Переиспользуемые компоненты
    │   ├── ScreenContainer.tsx     # Обёртка для экранов
    │   └── PrimaryButton.tsx       # Основная кнопка
    │
    └── screens/                # 📱 Экраны приложения
        │
        ├── onboarding/         # Онбординг
        │   ├── WelcomeScreen.tsx      # Приветствие
        │   ├── ConsentScreen.tsx      # Согласие на обработку данных
        │   └── ProfileSetupScreen.tsx # Настройка профиля
        │
        ├── DiaryScreen.tsx     # Главный дневник
        ├── GraphScreen.tsx     # График ПСВ
        ├── HistoryScreen.tsx   # История измерений
        └── SettingsScreen.tsx  # Настройки
```

## 🎯 Компоненты по категориям

### Навигация
- `AppNavigator.tsx` - управляет переходами между экранами
  - RootStack - корневой навигатор
  - OnboardingStack - стек онбординга
  - MainTab - нижняя панель с вкладками

### Модели данных (types/models.ts)
```typescript
- TimePeriod: 'morning' | 'evening'
- Profile: пол, год рождения, рост, метод нормы
- PEFRecord: ID, дата, период, значение ПСВ, симптомы
```

### Сервисы (services/)

**Storage.ts** - управление локальным хранилищем:
- `getProfile()` - получить профиль
- `saveProfile()` - сохранить профиль
- `getAllRecords()` - получить все записи
- `saveRecord()` - сохранить запись
- `deleteRecord()` - удалить запись
- `clearAll()` - очистить всё
- `hasCompletedOnboarding()` - проверка онбординга
- `setOnboardingCompleted()` - завершить онбординг

**NotificationsService.ts** - заглушка для будущих уведомлений

### Общие компоненты (components/)

**ScreenContainer** - обёртка для всех экранов:
- SafeAreaView
- Опциональный заголовок
- Scrollable/non-scrollable режимы
- Единый стиль

**PrimaryButton** - стилизованная кнопка:
- Анимация нажатия
- Disabled состояние
- Кастомные стили

### Экраны онбординга (screens/onboarding/)

1. **WelcomeScreen** - приветствие
   - Описание приложения
   - Кнопка "Далее"

2. **ConsentScreen** - согласие
   - Информация о хранении данных
   - Чекбокс согласия
   - Кнопка "Продолжить" (активна при согласии)

3. **ProfileSetupScreen** - настройка профиля
   - Выбор пола (М/Ж)
   - Ввод года рождения
   - Ввод роста в см
   - Выбор метода расчёта нормы (авто/вручную)
   - Ввод нормы вручную (если выбрано)
   - Валидация всех полей
   - Сохранение в Storage

### Основные экраны (screens/)

1. **DiaryScreen** (вкладка "Дневник")
   - Заглушка для формы ввода
   - Кнопка "Добавить запись"

2. **GraphScreen** (вкладка "График")
   - Описание зон (зелёная/жёлтая/красная)
   - Место для будущего графика

3. **HistoryScreen** (вкладка "История")
   - Описание функционала
   - Место для списка записей

4. **SettingsScreen** (вкладка "Настройки")
   - Информация о приложении
   - Кнопка "Очистить все данные"

## 🔄 Поток навигации

```
Запуск приложения
    ↓
[Проверка hasOnboarded]
    ↓
    ├─→ FALSE → OnboardingStack
    │              ↓
    │         WelcomeScreen
    │              ↓
    │         ConsentScreen
    │              ↓
    │         ProfileSetupScreen
    │              ↓
    │         [Сохранить профиль + setOnboardingCompleted]
    │              ↓
    └─→ TRUE  → MainTab (Bottom Tabs)
                   ├── Дневник
                   ├── График
                   ├── История
                   └── Настройки
```

## 💾 Хранение данных

### AsyncStorage ключи:
- `profile` - профиль пользователя (JSON)
- `records` - массив записей ПСВ (JSON)
- `hasOnboarded` - флаг завершения онбординга ('true'/'false')

### Формат данных:

**Profile:**
```json
{
  "gender": "M",
  "birthYear": 1990,
  "heightCm": 175,
  "normMethod": "auto",
  "manualNormValue": null
}
```

**PEFRecord:**
```json
{
  "id": "uuid-here",
  "date": "2025-11-18",
  "timePeriod": "morning",
  "value": 450,
  "cough": false,
  "breathlessness": false,
  "sputum": false
}
```

## 🎨 Стилистика

### Цвета:
- Основной: `#007AFF` (iOS Blue)
- Текст: `#333` (тёмный), `#666` (средний), `#999` (светлый)
- Фон: `#fff` (белый)
- Границы: `#ddd`, `#e0e0e0`
- Опасность: `#F44336` (красный)
- Успех: `#4CAF50` (зелёный)
- Предупреждение: `#FFC107` (жёлтый)

### Типографика:
- Заголовок экрана: 28px, bold
- Заголовок секции: 20px, semibold
- Обычный текст: 15-16px, regular
- Мелкий текст: 14px

### Отступы:
- Стандартный padding: 20px
- Маленький отступ: 8-12px
- Большой отступ: 32px

## 📦 Зависимости

### Основные:
- `expo` - фреймворк
- `react` / `react-native` - основа
- `@react-navigation/native` - навигация
- `@react-navigation/native-stack` - стек
- `@react-navigation/bottom-tabs` - вкладки
- `@react-native-async-storage/async-storage` - хранилище

### Dev:
- `typescript` - типизация
- `@types/react` - типы для React

## ✅ Что готово

- ✅ Полная структура проекта
- ✅ Навигация (онбординг + основные экраны)
- ✅ Все экраны с UI-заглушками
- ✅ Локальное хранилище
- ✅ Типизация TypeScript
- ✅ Общие компоненты
- ✅ Утилиты для работы с датами
- ✅ Валидация форм

## 🚧 Следующие шаги для разработки

1. **DiaryScreen:**
   - Форма добавления записи
   - Выбор периода (утро/вечер)
   - Ввод значения ПСВ
   - Чекбоксы симптомов
   - Сохранение в Storage
   - Генерация UUID для записи

2. **GraphScreen:**
   - Установить библиотеку графиков (Victory или react-native-chart-kit)
   - Загрузка данных из Storage
   - Расчёт зон (зелёная/жёлтая/красная)
   - Отображение графика
   - Фильтр по периоду

3. **HistoryScreen:**
   - FlatList с записями
   - Сортировка по дате (новые сверху)
   - Фильтры (по периоду, по дате)
   - Свайп для удаления
   - Модальное окно редактирования

4. **Дополнительно:**
   - Расчёт нормы ПСВ по формулам
   - Локальные уведомления (expo-notifications)
   - Экспорт данных в PDF/CSV
   - Dark mode
   - Локализация (i18n)

## 🧪 Тестирование

Для тестирования рекомендуется:
1. Проверить онбординг с разными данными
2. Протестировать навигацию
3. Проверить сохранение/загрузку данных
4. Тестировать на iOS и Android
5. Проверить работу после перезапуска приложения

## 📱 Поддерживаемые платформы

- ✅ iOS (iPhone, iPad)
- ✅ Android
- ⚠️ Web (частично, для тестирования)

## 👨‍💻 Технические детали

- **Expo SDK:** 51.0.0
- **React:** 18.2.0
- **React Native:** 0.74.5
- **TypeScript:** 5.1.3
- **Target:** iOS 13+, Android 5+



