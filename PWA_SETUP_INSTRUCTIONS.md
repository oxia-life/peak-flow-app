# Инструкция по настройке PWA для Дневника пикфлоуметрии

## ✅ Что уже сделано

1. **Обновлен `app.json`** с настройками PWA
2. **Создан `manifest.json`** для настройки веб-приложения
3. **Создан кастомный `index.html`** с мета-тегами для iOS и Android
4. **Создан `service-worker.js`** для оффлайн работы

## 📱 Что нужно сделать

### 1. Создать иконки приложения

Вам нужно создать иконки для PWA в следующих размерах:

- **192x192 пикселей** → сохранить как `public/icon-192.png`
- **512x512 пикселей** → сохранить как `public/icon-512.png`
- **180x180 пикселей** (для iOS) → можно использовать icon-192.png

**Где взять иконки:**
1. Если у вас уже есть `assets/icon.png`:
   - Откройте его в любом графическом редакторе
   - Измените размер до 192x192 и сохраните в `public/icon-192.png`
   - Измените размер до 512x512 и сохраните в `public/icon-512.png`

2. Или используйте онлайн-генератор:
   - https://www.pwabuilder.com/imageGenerator
   - https://realfavicongenerator.net/

### 2. Обновить Expo конфигурацию

Запустите команду для применения изменений:

```bash
npx expo export:web
```

### 3. Протестировать PWA локально

1. Запустите веб-версию:
```bash
npm run web
```

2. Откройте в браузере: `http://localhost:19006`

3. **Тестирование на мобильных устройствах:**
   - **Android (Chrome):**
     1. Откройте сайт в Chrome
     2. Нажмите на меню (три точки)
     3. Выберите "Добавить на главный экран"
     4. Подтвердите установку
   
   - **iOS (Safari):**
     1. Откройте сайт в Safari
     2. Нажмите кнопку "Поделиться" (квадрат со стрелкой вверх)
     3. Прокрутите вниз и выберите "На экран «Домой»"
     4. Нажмите "Добавить"

### 4. Проверка PWA

Откройте DevTools (F12) → вкладка **Application** (или **Приложение**):

- **Manifest**: проверьте, что manifest.json загружен корректно
- **Service Workers**: убедитесь, что Service Worker активен
- **Lighthouse**: запустите аудит PWA (во вкладке Lighthouse)

### 5. Деплой на хостинг

После локального тестирования разверните приложение на хостинг:

**Рекомендуемые платформы:**
- **Vercel** (бесплатно): https://vercel.com
- **Netlify** (бесплатно): https://netlify.com
- **GitHub Pages** (бесплатно)
- **Firebase Hosting** (бесплатно)

**Команды для деплоя на Vercel:**
```bash
# Установить Vercel CLI
npm i -g vercel

# Собрать веб-версию
npx expo export:web

# Задеплоить
cd web-build
vercel --prod
```

## 🎨 Настройка брендинга

В файле `app.json` и `manifest.json` уже настроены:
- **Название**: "Дневник пикфлоуметрии"
- **Короткое название**: "ПСВ Дневник"
- **Цвет темы**: `#1E4C60` (синий из вашего дизайна)
- **Фон**: `#ffffff` (белый)

При желании вы можете изменить эти значения.

## 🔧 Устранение неполадок

### Проблема: PWA не устанавливается

**Решение:**
1. Убедитесь, что сайт открыт по **HTTPS** (или localhost)
2. Проверьте, что `manifest.json` доступен по адресу `/manifest.json`
3. Проверьте, что все иконки существуют и доступны
4. Откройте DevTools → Console и проверьте ошибки

### Проблема: Service Worker не регистрируется

**Решение:**
1. Проверьте консоль на ошибки
2. Убедитесь, что `service-worker.js` доступен по адресу `/service-worker.js`
3. Попробуйте очистить кэш браузера (Ctrl+Shift+Delete)

### Проблема: На iOS не показывается кастомная иконка

**Решение:**
1. Убедитесь, что добавлена мета-тег `apple-touch-icon`
2. Иконка должна быть PNG без прозрачности
3. Размер иконки должен быть 180x180 или 192x192

## 📚 Дополнительные ресурсы

- [Expo Web Documentation](https://docs.expo.dev/workflow/web/)
- [PWA Checklist](https://web.dev/pwa-checklist/)
- [MDN: Progressive Web Apps](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps)

## ✨ Что получит пользователь

После установки PWA на главный экран:
- ✅ Приложение открывается в полноэкранном режиме (без адресной строки браузера)
- ✅ Свой значок на главном экране
- ✅ Быстрая загрузка благодаря Service Worker
- ✅ Работа офлайн (для ранее загруженных страниц)
- ✅ Выглядит и работает как нативное приложение

---

**Следующий шаг:** Создайте иконки и протестируйте PWA локально! 🚀


