# Дневник пикфлоуметрии

Мобильное приложение для отслеживания показателей пиковой скорости выдоха (ПСВ) на iOS и Android.

## Технологический стек

- **React Native** с **Expo** (managed workflow)
- **TypeScript** для типобезопасности
- **React Navigation** (Stack + Bottom Tabs)
- **AsyncStorage** для локального хранения данных

## Структура проекта

```
src/
├── AppNavigator.tsx          # Корневой навигатор
├── components/               # Общие компоненты
│   ├── PrimaryButton.tsx
│   └── ScreenContainer.tsx
├── screens/                  # Экраны приложения
│   ├── onboarding/          # Онбординг
│   │   ├── WelcomeScreen.tsx
│   │   ├── ConsentScreen.tsx
│   │   └── ProfileSetupScreen.tsx
│   ├── DiaryScreen.tsx      # Главный дневник
│   ├── GraphScreen.tsx      # График ПСВ
│   ├── HistoryScreen.tsx    # История измерений
│   └── SettingsScreen.tsx   # Настройки
├── services/                 # Сервисы
│   ├── Storage.ts           # Работа с AsyncStorage
│   └── NotificationsService.ts
├── types/                    # TypeScript типы
│   └── models.ts
└── utils/                    # Утилиты
    └── dateUtils.ts
```

## Установка и запуск

### Предварительные требования

- Node.js (v18 или выше)
- npm или yarn
- Expo CLI (устанавливается автоматически)

### Шаги установки

1. **Установите зависимости:**

```bash
npm install
```

или

```bash
yarn install
```

2. **Запустите проект:**

```bash
npm start
```

или

```bash
expo start
```

3. **Запуск на устройстве:**

- **iOS:** Нажмите `i` в терминале или отсканируйте QR-код приложением Expo Go на iPhone
- **Android:** Нажмите `a` в терминале или отсканируйте QR-код приложением Expo Go на Android
- **Web:** Нажмите `w` в терминале (для тестирования)

### Сборка для продакшена

```bash
# iOS
expo build:ios

# Android
expo build:android
```

## Функциональность

### Реализовано

✅ Структура проекта  
✅ Навигация (онбординг → основные экраны)  
✅ Онбординг с 3 шагами:
  - Приветствие
  - Согласие на обработку данных
  - Настройка профиля (пол, год рождения, рост, метод расчета нормы)
✅ 4 основных экрана с заглушками:
  - Дневник измерений
  - График ПСВ
  - История
  - Настройки
✅ Локальное хранилище (AsyncStorage)  
✅ Типизация TypeScript

### В планах (для дальнейшей разработки)

- 📝 Добавление записей измерений ПСВ
- 📊 Реализация графика с зонами (зелёная/жёлтая/красная)
- 📋 Список истории с фильтрацией
- 🔔 Напоминания о измерениях
- 🧮 Автоматический расчет нормы ПСВ
- 📤 Экспорт данных (PDF, CSV)
- 🌐 Локализация

## Хранение данных

Все данные хранятся локально на устройстве пользователя с помощью AsyncStorage. Данные **не передаются** на внешние серверы.

### Структура данных

**Profile:**
- Пол (M/F)
- Год рождения
- Рост (см)
- Метод расчёта нормы (авто/вручную)
- Значение нормы (если вручную)

**PEFRecord:**
- ID (uuid)
- Дата (ISO YYYY-MM-DD)
- Период (утро/вечер)
- Значение ПСВ (л/мин)
- Симптомы: кашель, одышка, мокрота

## Разработка

Проект готов к дальнейшему расширению. Каждый экран имеет заглушку, которую можно постепенно дорабатывать через отдельные задачи.

### Следующие шаги для разработки:

1. Реализовать форму добавления записи ПСВ в DiaryScreen
2. Добавить библиотеку графиков (react-native-chart-kit или Victory)
3. Реализовать список истории с возможностью редактирования
4. Добавить локальные уведомления (expo-notifications)
5. Реализовать расчёт нормы ПСВ по формулам

## Лицензия

Приватный проект для Oxia



