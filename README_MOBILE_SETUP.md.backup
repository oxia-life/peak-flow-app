# 🚀 Peak Flow Diary - MONO-REPO Setup

## ✅ **ЧТО УЖЕ ГОТОВО:**

- [x] ✅ EAS конфигурация (`eas.json`)
- [x] ✅ App конфигурация (`app.config.js`)
- [x] ✅ Безопасный `.gitignore` (защита credentials)
- [x] ✅ Скрипт `safe-push.sh` (защита от случайного деплоя)
- [x] ✅ Cursor Rules (`.cursorules`)
- [x] ✅ Структура веток:
  - `main` → веб (автодеплой на peakflow.oxia.life) ✅
  - `mobile/staging` → разработка мобильных ✅
  - `mobile/production` → релизы в App Store / Google Play ✅
- [x] ✅ NPM команды для мобильных сборок
- [x] ✅ Документация:
  - `BRANCH_STRATEGY.md` - стратегия работы с ветками
  - `MOBILE_WORKFLOW.md` - правила и команды
  - `SETUP_MOBILE.md` - пошаговая инструкция

---

## 🔴 **КРИТИЧЕСКОЕ ПРАВИЛО:**

```
main ветка = ТОЛЬКО ВЕБ
```

**Любой push в `main` → автоматический деплой на `peakflow.oxia.life`**

---

## 🎯 **ЧТО ДЕЛАТЬ ДАЛЬШЕ:**

### **ШАГ 1: Установите EAS CLI**

```bash
npm install -g eas-cli
```

### **ШАГ 2: Войдите в Expo**

```bash
eas login
```

### **ШАГ 3: Инициализируйте EAS**

```bash
cd /Users/monastyrskaya/Documents/peak-flow-diary
eas build:configure
```

Это обновит `app.config.js` с вашим `projectId`.

### **ШАГ 4: Зарегистрируйтесь в Google Play**

1. https://play.google.com/console
2. Create Developer Account ($25)
3. Следуйте инструкциям в `SETUP_MOBILE.md` → ЭТАП 4

### **ШАГ 5: Настройте App Store Connect**

1. https://appstoreconnect.apple.com (у вас уже есть аккаунт ✅)
2. My Apps → + → New App
3. Следуйте инструкциям в `SETUP_MOBILE.md` → ЭТАП 5

### **ШАГ 6: Первая тестовая сборка**

```bash
# Переключитесь на mobile/staging
git checkout mobile/staging

# Соберите preview билд
npm run mobile:preview
```

### **ШАГ 7: Изучите документацию**

Откройте в Cursor:
- 📖 `SETUP_MOBILE.md` - **НАЧНИТЕ С ЭТОГО!**
- 📖 `BRANCH_STRATEGY.md` - как работать с ветками
- 📖 `MOBILE_WORKFLOW.md` - команды и правила

---

## 🌳 **СТРУКТУРА ВЕТОК:**

```
peak-flow-diary/
├── main                    ✅ ВЕБ (peakflow.oxia.life)
│   └── git push → автодеплой
│
├── mobile/staging          🚀 Мобильная разработка
│   └── eas build --profile preview
│
└── mobile/production       📱 Релизы в сторы
    └── eas build --profile production
```

---

## 📱 **КОМАНДЫ:**

### **Разработка:**
```bash
npm run web           # Тестирование веба
npm start             # Тестирование мобильных (Expo Go)
npm run ios           # iOS simulator
npm run android       # Android emulator
```

### **Мобильные сборки:**
```bash
npm run mobile:preview           # Тестовая сборка (preview)
npm run mobile:build:ios         # iOS продакшн
npm run mobile:build:android     # Android продакшн
npm run mobile:build:all         # Все платформы
```

### **Публикация:**
```bash
npm run mobile:submit:ios        # Загрузка в App Store
npm run mobile:submit:android    # Загрузка в Google Play
npm run mobile:submit:all        # Загрузка в оба стора
```

### **Безопасность:**
```bash
npm run safe-push     # Безопасный push с подтверждением
npm run check:branch  # Проверка текущей ветки
```

---

## 🎨 **СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ:**

### **Сценарий 1: Правка бага в вебе**

```bash
# Находитесь в main
git checkout main

# Вносите изменения в Cursor

# Тестируете
npm run web

# Коммитите
git add .
git commit -m "Web: Fix settings bug"

# Пушите (можно использовать safe-push)
npm run safe-push
# или
git push origin main
```

✅ **Результат:** Веб обновился, мобильные ветки не затронуты

---

### **Сценарий 2: Новая фича для мобильных**

```bash
# Переключаемся на mobile/staging
git checkout mobile/staging

# Синхронизируем с main (берем последние общие изменения)
git merge main

# Вносите изменения в Cursor
# Используете Platform.OS проверки:
if (Platform.OS !== 'web') {
  // Код только для мобильных
}

# Тестируете на телефоне
npm start

# Коммитите
git add .
git commit -m "Mobile: Add push notifications"
git push origin mobile/staging

# Собираете превью билд
npm run mobile:preview
```

✅ **Результат:** main не затронут, веб продолжает работать

---

### **Сценарий 3: Релиз в App Store / Google Play**

```bash
# 1. Мержите в production
git checkout mobile/production
git merge mobile/staging

# 2. Обновите версию в app.config.js
# version: "1.0.1" → "1.1.0"
# buildNumber: "2" (iOS)
# versionCode: 2 (Android)

# 3. Коммитите
git add app.config.js
git commit -m "Mobile: Bump version to 1.1.0"
git push origin mobile/production

# 4. Собираете продакшн билд
npm run mobile:build:all

# 5. Загружаете в сторы
npm run mobile:submit:all

# 6. Тегаете релиз
git tag -a mobile-v1.1.0 -m "Mobile Release 1.1.0"
git push origin mobile-v1.1.0

# 7. Возвращаетесь в main
git checkout main
```

✅ **Результат:** Приложения обновятся в сторах, веб не затронут

---

## 🔒 **БЕЗОПАСНОСТЬ:**

### **Защита от случайного деплоя:**

1. **Скрипт `safe-push.sh`:**
   ```bash
   npm run safe-push
   ```
   Спросит подтверждение перед push в `main`.

2. **Cursor Rules (`.cursorules`):**
   - Автоматически подсказывает правила работы
   - Напоминает проверить `Platform.OS` перед коммитом

3. **`.gitignore`:**
   - Защищает `google-play-service-account.json`
   - Игнорирует мобильные билды

---

## 📂 **PLATFORM-SPECIFIC КОД:**

### **Способ 1: Runtime проверка**

```typescript
import { Platform } from 'react-native';

if (Platform.OS === 'web') {
  // Веб-специфичный код
} else if (Platform.OS === 'ios') {
  // iOS-специфичный код
} else {
  // Android-специфичный код
}
```

### **Способ 2: Отдельные файлы**

```
src/components/
├── ShareButton.tsx          # Общий интерфейс
├── ShareButton.web.tsx      # Автоматически используется на вебе
├── ShareButton.ios.tsx      # Автоматически используется на iOS
└── ShareButton.android.tsx  # Автоматически используется на Android
```

React Native автоматически выберет нужный файл!

---

## 📚 **ДОКУМЕНТАЦИЯ:**

| Файл | Описание |
|------|----------|
| **SETUP_MOBILE.md** | **⭐ НАЧНИТЕ ЗДЕСЬ!** Пошаговая инструкция запуска в App Store / Google Play |
| **BRANCH_STRATEGY.md** | Детальные сценарии работы с ветками |
| **MOBILE_WORKFLOW.md** | Правила и команды |
| **.cursorules** | Правила для Cursor AI |

---

## 🔍 **ПРОВЕРКА ТЕКУЩЕЙ ВЕТКИ:**

В Cursor внизу экрана всегда видна текущая ветка:

```
✅ main          → можно пушить (только веб-изменения!)
✅ mobile/*      → можно пушить (мобильные изменения)
⚠️  другие       → проверьте перед push
```

Или используйте:
```bash
npm run check:branch
```

---

## 🎉 **ГОТОВО К РАБОТЕ!**

Система настроена и защищена. Теперь можно безопасно:

1. ✅ Разрабатывать веб в `main`
2. ✅ Разрабатывать мобильные в `mobile/staging`
3. ✅ Релизить мобильные из `mobile/production`
4. ✅ Не бояться случайно сломать одно при изменении другого

---

## 🆘 **ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК:**

### **Случайно запушили мобильные изменения в main:**

```bash
# Откатите последний коммит
git reset --hard HEAD~1

# Или откатите до конкретного коммита
git reset --hard <commit-hash>

# Форс-пуш (ОСТОРОЖНО!)
git push --force origin main
```

### **Нужна помощь:**

1. 📖 Откройте `SETUP_MOBILE.md` → TROUBLESHOOTING
2. 🔍 Проверьте `.cursorules` → там есть подсказки
3. 📚 Изучите `BRANCH_STRATEGY.md` → детальные сценарии

---

## 📊 **СТАТУС ПРОЕКТА:**

| Компонент | Статус | URL |
|-----------|--------|-----|
| **Веб** | ✅ Работает | https://peakflow.oxia.life |
| **iOS** | ⏳ Настраивается | App Store Connect |
| **Android** | ⏳ Настраивается | Google Play Console |
| **EAS Config** | ✅ Готов | `eas.json` |
| **Ветки** | ✅ Созданы | main, mobile/staging, mobile/production |
| **Документация** | ✅ Готова | SETUP_MOBILE.md, BRANCH_STRATEGY.md |

---

## 🚀 **СЛЕДУЮЩИЕ ШАГИ:**

1. [ ] Установите EAS CLI: `npm install -g eas-cli`
2. [ ] Войдите в Expo: `eas login`
3. [ ] Инициализируйте проект: `eas build:configure`
4. [ ] Зарегистрируйтесь в Google Play ($25)
5. [ ] Настройте App Store Connect (уже есть аккаунт ✅)
6. [ ] Первая сборка: `git checkout mobile/staging && npm run mobile:preview`
7. [ ] Изучите `SETUP_MOBILE.md` для детальных инструкций

---

## 💡 **ПОЛЕЗНЫЕ ССЫЛКИ:**

- **Expo Docs:** https://docs.expo.dev
- **EAS Build:** https://docs.expo.dev/build/introduction/
- **App Store Connect:** https://appstoreconnect.apple.com
- **Google Play Console:** https://play.google.com/console
- **Privacy Policy:** https://oxia.life/page97307316.html

---

Удачи с запуском мобильных приложений! 🎉

