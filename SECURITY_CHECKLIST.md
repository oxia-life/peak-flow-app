# 🔒 Чек-лист безопасности для публикации

## ⚠️ КРИТИЧНО - Проверьте перед деплоем!

### 1. Защита секретных данных

- [ ] Файл `.env` находится в `.gitignore`
- [ ] Файл `.env` НЕ закоммичен в Git
- [ ] Проверено командой: `git log --all -- .env` (должно быть пусто)
- [ ] В коде нет хардкодов API ключей или паролей
- [ ] Используются только `EXPO_PUBLIC_*` переменные для клиента

**Проверка:**
```bash
# Проверьте .gitignore
grep ".env" .gitignore

# Проверьте историю Git
git log --all --full-history -- .env

# Найдите хардкоды (не должно быть результатов)
grep -r "supabase" src/ | grep -v "EXPO_PUBLIC"
```

---

### 2. Настройка Supabase (КРИТИЧНО!)

- [ ] **Row Level Security (RLS) включен** для всех таблиц
- [ ] Политики RLS проверены и корректны
- [ ] Anon key используется только на клиенте
- [ ] Service role key НЕ используется нигде в коде
- [ ] Redirect URLs обновлены для `peakflow.oxia.life`

**Проверка RLS в Supabase SQL Editor:**
```sql
-- Все таблицы должны иметь RLS = true
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'public';

-- Проверьте политики
SELECT schemaname, tablename, policyname, permissive, roles, cmd, qual
FROM pg_policies
WHERE schemaname = 'public';
```

**ВАЖНО:** Если RLS выключен — **это критическая уязвимость!** Любой пользователь сможет видеть данные других пользователей.

---

### 3. Переменные окружения на хостинге

- [ ] Переменные добавлены в Vercel/Netlify Dashboard
- [ ] Используется `EXPO_PUBLIC_SUPABASE_URL`
- [ ] Используется `EXPO_PUBLIC_SUPABASE_ANON_KEY`
- [ ] НЕ используется Service Role Key в переменных

**Где добавить:**
- **Vercel:** Settings → Environment Variables
- **Netlify:** Site settings → Environment variables

---

### 4. HTTPS и SSL

- [ ] Сайт доступен только по HTTPS
- [ ] SSL-сертификат валиден
- [ ] HTTP автоматически редиректит на HTTPS
- [ ] HSTS заголовок настроен

**Проверка:**
```bash
# Проверьте SSL
curl -I https://peakflow.oxia.life

# Должно быть:
# HTTP/2 200
# strict-transport-security: max-age=63072000
```

**Проверка SSL качества:**
https://www.ssllabs.com/ssltest/analyze.html?d=peakflow.oxia.life

**Цель:** Оценка **A** или **A+**

---

### 5. Заголовки безопасности

- [ ] `vercel.json` или `netlify.toml` настроены
- [ ] Content-Security-Policy включен
- [ ] X-Frame-Options = DENY
- [ ] X-Content-Type-Options = nosniff
- [ ] X-XSS-Protection включен
- [ ] Referrer-Policy настроен

**Проверка заголовков:**
```bash
curl -I https://peakflow.oxia.life
```

**Онлайн проверка:**
https://securityheaders.com/?q=peakflow.oxia.life

**Цель:** Оценка **A** или выше

---

### 6. Аутентификация и авторизация

- [ ] Magic Link работает с новым доменом
- [ ] Redirect URLs обновлены в Supabase
- [ ] Email templates проверены (если кастомные)
- [ ] Тестовая авторизация прошла успешно

**Redirect URLs в Supabase должны включать:**
```
https://peakflow.oxia.life
https://peakflow.oxia.life/**
```

---

### 7. Защита данных пользователей

- [ ] RLS политики тестированы
- [ ] Пользователь не может видеть данные других пользователей
- [ ] Пользователь не может редактировать данные других пользователей
- [ ] Удаление данных работает только для своего профиля

**Тестирование:**
1. Создайте 2 тестовых аккаунта
2. Попробуйте получить данные другого пользователя через DevTools → Network
3. Должна быть ошибка 403 или 401

---

### 8. PWA и Service Worker

- [ ] Service Worker регистрируется корректно
- [ ] manifest.json доступен
- [ ] Иконки загружаются
- [ ] PWA устанавливается на телефон
- [ ] Offline режим работает (для кэшированных страниц)

**Проверка в DevTools:**
- Application → Manifest (должен загрузиться)
- Application → Service Workers (должен быть активен)
- Application → Cache Storage (должен заполниться)

---

### 9. Git и репозиторий

- [ ] `.env` в `.gitignore`
- [ ] `node_modules/` в `.gitignore`
- [ ] `web-build/` в `.gitignore`
- [ ] Нет коммитов с секретами
- [ ] Репозиторий приватный (если содержит чувствительные данные)

**Очистка истории Git (если .env был закоммичен):**
```bash
# ВНИМАНИЕ: Это изменит историю!
git filter-branch --index-filter \
  "git rm -rf --cached --ignore-unmatch .env" HEAD
```

Затем **обязательно смените ключи в Supabase!**

---

### 10. Мониторинг и логирование

- [ ] Настроены уведомления о сбоях в Vercel/Netlify
- [ ] Настроен мониторинг в Supabase
- [ ] Console.log удалены или заменены на production-ready логирование
- [ ] Ошибки не раскрывают внутреннюю структуру

---

### 11. Производительность и доступность

- [ ] Lighthouse аудит PWA (оценка > 90)
- [ ] Время загрузки < 3 секунд
- [ ] Приложение работает на медленном интернете
- [ ] Приложение адаптивно на разных экранах

**Запуск Lighthouse:**
1. DevTools → Lighthouse
2. Выберите "Progressive Web App"
3. Generate report
4. Цель: > 90 баллов

---

### 12. Соответствие GDPR / Защита персональных данных

**Для медицинского приложения критично:**

- [ ] Добавлена политика конфиденциальности
- [ ] Пользователь согласен на обработку данных
- [ ] Есть возможность удалить все данные (уже реализована)
- [ ] Данные хранятся в зашифрованном виде (Supabase)
- [ ] Нет передачи данных третьим лицам

**Где добавить политику конфиденциальности:**
1. Создайте страницу `/privacy` или `/privacy-policy`
2. Добавьте ссылку в настройках приложения
3. При первом входе попросите согласие пользователя

---

## 🚨 Критические уязвимости, которых НЕ должно быть

### ❌ НЕ ДЕЛАЙТЕ ТАК:

1. **Хардкод API ключей в коде:**
   ```javascript
   // ❌ ПЛОХО
   const supabaseUrl = 'https://xxxxx.supabase.co'
   
   // ✅ ХОРОШО
   const supabaseUrl = process.env.EXPO_PUBLIC_SUPABASE_URL
   ```

2. **RLS выключен:**
   ```sql
   -- ❌ ПЛОХО
   ALTER TABLE profiles DISABLE ROW LEVEL SECURITY;
   
   -- ✅ ХОРОШО
   ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
   ```

3. **Использование Service Role Key на клиенте:**
   ```javascript
   // ❌ ПЛОХО - Service Role обходит RLS!
   const supabase = createClient(url, SERVICE_ROLE_KEY)
   
   // ✅ ХОРОШО
   const supabase = createClient(url, ANON_KEY)
   ```

4. **Отсутствие HTTPS:**
   ```
   ❌ http://peakflow.oxia.life
   ✅ https://peakflow.oxia.life
   ```

5. **Доступ к данным других пользователей:**
   ```sql
   -- ❌ ПЛОХО - любой может читать
   CREATE POLICY "Anyone can read" ON profiles FOR SELECT USING (true);
   
   -- ✅ ХОРОШО - только свои данные
   CREATE POLICY "Users can read own data" ON profiles FOR SELECT 
   USING (auth.uid() = user_id);
   ```

---

## ✅ Финальная проверка перед запуском

Выполните все команды:

```bash
# 1. Проверка .env
cat .gitignore | grep .env
git log --all -- .env

# 2. Сборка
npm run build:web

# 3. Проверка сборки
ls -la web-build/

# 4. Тест безопасности (после деплоя)
curl -I https://peakflow.oxia.life
```

---

## 🆘 Что делать при обнаружении уязвимости

### Если .env попал в Git:

1. **НЕМЕДЛЕННО** смените ключи в Supabase:
   - Project Settings → API → Reset anon key

2. Очистите историю Git:
   ```bash
   git filter-branch --index-filter \
     "git rm -rf --cached --ignore-unmatch .env" HEAD
   git push --force
   ```

3. Обновите `.env` с новыми ключами

### Если RLS выключен:

1. **НЕМЕДЛЕННО** включите RLS:
   ```sql
   ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
   ALTER TABLE pef_records ENABLE ROW LEVEL SECURITY;
   ALTER TABLE user_settings ENABLE ROW LEVEL SECURITY;
   ```

2. Создайте политики (см. `SUPABASE_SETUP.md`)

3. Протестируйте доступ

---

## 📞 Контрольный список после деплоя

**День 1:**
- [ ] Протестируйте все функции
- [ ] Проверьте логи на ошибки
- [ ] Тест безопасности

**Неделя 1:**
- [ ] Мониторинг производительности
- [ ] Проверка отзывов пользователей
- [ ] Анализ логов Supabase

**Месяц 1:**
- [ ] Обновление зависимостей
- [ ] Проверка безопасности
- [ ] Бэкап данных

---

## ✨ Готово!

Если все пункты отмечены ✅ — ваше приложение безопасно и готово к запуску!

**Помните:**
- Безопасность — это процесс, а не одноразовая задача
- Регулярно обновляйте зависимости
- Следите за логами
- Реагируйте на подозрительную активность

**Удачного запуска! 🚀**


